# dotfiles

Minimal asbrctl's dotfiles

current link files:

 - ~/.profile
 - ~/.bash_profile
 - ~/.bashrc
 - ~/.bash_aliases
 - ~/.screenrc
 - ~/.vimrc
 - ~/.tmux.conf
 - ~/tmux/t1-fwd 

After cloned the repository run linkFiles.sh
